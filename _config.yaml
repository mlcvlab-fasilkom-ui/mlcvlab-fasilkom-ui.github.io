# site properties and page defaults
title: MACHINE LEARNING AND COMPUTER VISION 「 MLCV 」 LAB
subtitle: FACULTY OF COMPUTER SCIENCE, UNIVERSITAS INDONESIA
description: The MLCV Lab focuses on research related to manipulating, analyzing, and interpreting data in the form of images or pictures. The methods used are very diverse, ranging from processing detailed information stored at the pixel level to geometrically dividing images into several regions to form patterns.
header: images/background.jpg
footer: images/background.jpg
header-dark: false
footer-dark: false
proofer: false

# site social media and other links
links:
  email: mlcvlab@cs.ui.ac.id
  github: mlcvlab-fasilkom-ui
  # orcid: 0000-0001-8713-9213
  # google-scholar: ETJoidYAAAAJ
  # twitter: mlcvlab-fasilkom-ui
  # youtube: mlcvlab-fasilkom-ui

### jekyll settings

# front matter defaults
defaults:
  # all markdown files
  - scope:
      path: ""
    values:
      layout: default
  # markdown files in /_members
  - scope:
      type: "members"
    values:
      layout: member
  # markdown files in /_posts
  - scope:
      type: "posts"
    values:
      layout: post

collections:
  # generate page for each member
  members:
    output: true
  # generate page for each post
  posts:
    output: true

# jekyll plugins
plugins:
  - jekyll-spaceship
  - jekyll-sitemap
  - jekyll-redirect-from
  - jekyll-feed
  - jekyll-last-modified-at

# code block syntax highlighting
highlighter: rouge

# jekyll theme
theme: null

# sass settings
sass:
  sass_dir: _styles

# force jekyll to include certain files/folders
include:
  - _styles
  - _scripts

# force jekyll to exclude certain files/folders
exclude:
  - README.md
  - LICENSE.md
  - CITATION.cff
